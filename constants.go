package config

// Tag keys used for configuration.
const (
	EnvTag      = "env"
	RequiredTag = "required"
	DefaultTag  = "default"
	MinTag      = "min"
	MaxTag      = "max"
	RangeErrTag = "range_error"
)

// Common tag values.
const (
	tagTrue = "true"
)
